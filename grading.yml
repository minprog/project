grades:
    final:
        type: float
        subgrades:
            code: integer
            repo: integer
            design: integer
            docs: integer
        calculation: ((code.to_i+repo.to_i+design.to_i+docs.to_i).to_f/20*9+1).round(1)
        hide_calculated: true

calculation:
    eindcijfer:
        final: 1

final:
    submits:
        final: 1
